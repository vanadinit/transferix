[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "transferix"
version = "4.0.0"
requires-python = ">=3.10"
dependencies = [
    "Flask",
    "pynacl",
]

[tool.setuptools]
packages = ["transferix"]
